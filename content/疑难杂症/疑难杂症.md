# RDP

1. [注意端口](RDP#1)
2. [如果远程是用微软邮箱账户登陆的](RDP#2)
# ssh

1. 开启root登录：`/etc/ssh/sshd_config`，打开 `PermitRootLogin yes` 

# linux

1. 添加sudo权限：`su -`，`sudo usermod -aG sudo diraw`，然后重新登陆

# ventory

1. 格式化了硬盘之后，没有分配盘符，导致无法识别启动，需要去 `磁盘管理器` 里手动分配